spring:
  application:
    name: turing
  datasource:
    url: jdbc:mysql://zephyr.proxy.rlwy.net:43560/railway?createDatabaseIfNotExist=true
    username: root
    password: WLaNUvMrhFHFIZFbWCAHuGGzPKgPtjHV
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
api:
  security:
    token:
      secret: minha-chave-super-secreta-do-projeto-turing-2026

admin:
  seed:
    email: ${ADMIN_EMAIL:adm@gmail.com}
    senha: ${ADMIN_SENHA:123}
    cpf: ${ADMIN_CPF:00000000000}

